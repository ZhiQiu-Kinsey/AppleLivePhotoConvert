# 更新日志

本项目的版本历史与变更说明。

## [4.0.0] - 2026-09-24

本版本重写了转换引擎与桌面端，界面改为「图库 + 检查器」工作台，新增任务中心、HDR 保真与校验安装的依赖引擎。设置文件会自动迁移，但部分默认行为有变化，升级前请阅读「破坏性变化」。

### 新增

- **图库工作台**：一次扫描同时识别苹果实况对、安卓动态照片（Google / 小米 / 三星 / HEIC 动态照片）与普通照片，切换动作只筛选不重扫；等高排版按拍摄日、月、年分组，支持拍摄日期、创建日期、修改日期与文件名排序，小/中/大三档缩放与方形裁切。
- **检查器**：右侧面板集中四个动作（转为安卓、转为苹果、解包提取、空间瘦身）与各自参数，参数即时保存；可手动收起为窄条，窗口过窄时自动收起。
- **任务中心**：取代原「批次报告」页。运行中任务显示进度、吞吐与剩余时间，可暂停、继续与取消；本次运行的历史报告支持按结果筛选、定位文件、重试失败项与导出 CSV。
- **iPhone HDR 照片转为 Ultra HDR 动态照片**：合成时把 HEIC 中的 Apple HDR 增益图换算为 Ultra HDR（ISO 21496-1 与 Google hdrgm 两套元数据），支持的安卓相册按 HDR 显示。由检查器中的「保留 HDR」开关控制，默认开启；需要 heif-dec，缺少或源图无增益图时输出普通封面并在报告中注明原因。
- **HDR 视频**：悬浮播放与 QuickLook 对 HLG / PQ 视频做色调映射后显示；转码 HDR 视频时使用 libx265 10-bit 并保留色彩与 HDR10 元数据。FFmpeg 缺少所需滤镜或编码器时明确提示，不输出发灰画面。
- **瘦身对比**：对样张实际运行一次瘦身，用真实产物做卷帘对比，支持同步缩放、平移与 1:1 放大镜；相册级空间预估按抽样的真实压缩比外推。
- **依赖引擎**：内置依赖清单锁定 ExifTool 13.59、FFmpeg 7.0（npmmirror）/ 8.1.2（GitHub）、heif-enc 1.23.4；下载后校验 SHA256（npm 源另校验 integrity），解压时防路径穿越，试运行通过后整目录替换，失败或取消不留半成品。依赖页显示版本与 FFmpeg 能力（HDR 色调映射、HEVC 10-bit），已安装版本低于推荐版本时提示升级。
- **快捷键**：Ctrl+O 选择相册、F5 重新扫描、Enter 开始当前动作、Ctrl+1～4 切换页面、Ctrl+A 全选、Esc 取消选择或关闭弹窗、空格 QuickLook；QuickLook 中空格播放/暂停、左右方向键切换。
- **拖放与选择**：把文件夹拖入图库即可打开；单击单选、Ctrl 多选、Shift 范围选择，双击打开 QuickLook。
- **设置**：缩略图内存预算（64～1024 MB）与磁盘缓存上限（128 MB～16 GB），显示占用并可清理；记忆窗口位置、图库视图偏好与检查器布局。
- **稳定性**：未处理的界面异常写入日志并提示，不再直接退出；关闭窗口时若有任务运行会先确认，再取消任务并结束外部进程。
- **安装版与自动更新**：新增 Windows 安装版（按当前用户安装，无需管理员权限，带开始菜单与桌面快捷方式，可在「设置 → 应用」中卸载）与可自动更新的便携版；原解压版 ZIP 继续提供，但不自动更新。启动约 10 秒后在后台检查更新（每天最多一次，可关闭），发现新版本时弹窗显示版本、更新说明与下载大小（支持增量更新），可立即更新、稍后提醒或跳过此版本；有任务运行时可等任务完成后更新。偏好设置新增「更新」分组；有新版本时侧栏偏好设置显示小圆点，关于页显示「有新版本」。更新下载可走依赖页的 GitHub 加速镜像，清单与更新包逐级校验 SHA256。
- **发布**：Release 附带安装版、便携版、增量更新包、`SHA256SUMS.txt` 与构建来源证明；发布只由推送的版本 tag 触发。

### 改进

- **数据安全**：所有输出先写入目标目录内的暂存文件，校验通过后同卷重命名落盘；同一批次统一分配文件名，任何冲突策略下都不覆盖本批次的源文件；成对输出（HEIC + MOV）要么都落盘，要么都回滚。源文件的移动、回收或删除只在输出落盘并校验通过后执行。
- **缩略图**：按行高与屏幕缩放分档生成并缓存到磁盘，方向正确并统一转换到 sRGB；JPEG 采用缩放解码，4800 万像素照片解码峰值内存由约 275 MB 降到约 22 MB；内存按字节预算管理，正在显示的卡片不会被换成占位图；界面线程不再做文件读写。
- **播放**：安卓动态照片直接从照片内部读取视频，不再生成临时文件；按视频时间戳换帧，完整播放整段实况；全窗口只有一个悬浮播放器。300 组相册慢速滚动的峰值内存由约 895 MB 降到约 400 MB。
- **QuickLook**：按窗口实际像素加载高清图，画布尺寸固定，切换时按钮不再跳动。
- **瘦身**：转码后的 HEIC 不比原格式小时保留原格式，只剥离视频；带增益图的照片保留增益图并修正 MPF 信息。
- **配对**：扫描阶段直接使用合成时的配对校验，图库标记与任务结论一致；每张照片只对应一个条目；不依赖 ExifTool 即可读取拍摄时间与 ContentIdentifier。
- **本地化**：界面文案只来自两份资源字典；任务报告中的跳过与失败原因、依赖探测与安装失败原因按当前语言显示，切换语言后即时刷新；中英术语统一；「移入备份文件夹」的子文件夹名随界面语言（已合成 / 已拆分，Merged / Split）。
- **外观**：文字与图标对比度达到 WCAG AA，最小字号 11 px；深色主题下的弹窗改用主题配色。
- **设置**：修改即时生效并自动保存；写入为原子操作，无法解析的设置文件改名为 `settings.json.corrupt` 后使用默认值。
- **进度**：吞吐与剩余时间只按实际处理的项计算，并扣除暂停时长。
- **性能**：元数据读取改为 ExifTool 常驻会话池与批量 JSON；扫描时直接取文件大小与时间，大相册不再逐个查询文件信息。

### 修复

- 修复就地替换与覆盖输出在失败时可能丢失原片的问题；覆盖模式下成组落盘失败会还原原有文件。
- 修复瘦身只选中一张照片时仍处理整个目录的问题。
- 修复在新生成的 HEIC 上写入 ContentIdentifier 静默失败，导致还原后的实况对无法在 iPhone 上配对的问题。
- 修复 HEIC 动态照片与三星动态照片截断后残留视频容器头的问题。
- 修复 ExifTool 读取二进制数据时每次等待约 120 秒超时的问题。
- 修复 QuickTime 零时间（1904 年）被当成拍摄时间的问题。
- 修复预览过的安卓动态照片被当成实况对再次合成的问题。
- 修复竖拍照片在画廊中比例错误、EXIF 内嵌小缩略图被放大发糊、无权限子目录导致扫描中断、同一照片出现多张卡片、暂存与备份文件出现在画廊中的问题。
- 修复播放器释放后界面仍引用帧导致的崩溃，以及瘦身对比弹窗每次打开泄漏约 330 MB 内存的问题。
- 修复 HDR 视频转码为 8-bit 后发灰，以及 HEVC 输出标记为 hev1 导致 iOS 无法播放的问题。
- 修复依赖包解压的路径穿越风险，以及下载中断后留下损坏可执行文件的问题。
- 修复英文界面残留中文、永久删除确认按钮无法点击、关闭窗口时不取消任务等问题。

### 移除

- 移除「实况互转」「空间瘦身」「批次报告」三个独立页面，功能并入图库工作台与任务中心。
- 移除「连续试播全部可见照片」。
- 移除设置中从未生效的「自动下载依赖」选项。
- 不再依赖 ffprobe。
- 启动后自动清理旧版的缩略图缓存（`cache/thumbs`）与播放临时目录（`motion_cache`）。

### 破坏性变化

- **设置迁移**：设置文件（`%AppData%\LivePhotoConvert\settings.json`）升级到第 4 版并自动迁移：「覆盖同名文件」改为冲突策略，瘦身目录并入唯一的相册目录，新增缩略图预算与磁盘缓存上限。迁移是单向的，回退到 3.x 后部分设置需要重新选择。
- **依赖安装目录**：安装版与便携版把一键安装的工具放在 `%LocalAppData%\LivePhotoConvert\tools\<工具名>\`（更新会替换程序目录），程序目录中已有的工具会自动复制迁移；纯解压版放在程序目录的 `tools\<工具名>\` 下（不可写时同样落到 `%LocalAppData%`），版本由清单锁定。旧版安装在程序目录或 `tools\` 下的可执行文件仍能被识别，建议在依赖页按推荐版本重新安装。
- **卡片默认不选中**：未选中任何卡片时，动作作用于全部就绪项；拍摄时间不一致、待人工裁决的配对需要先裁决或显式选中才会处理。
- **缩略图缓存位置**：改为 `%LocalAppData%\LivePhotoConvert\cache\thumbnails`，首次启动会重新生成缩略图。
- **瘦身处理配对视频**：就地瘦身苹果实况对时，配对的 MOV 只在通过配对校验后移入回收站；导出模式不触碰 MOV。回收站仅在 Windows 上可用。

---

## [3.0.3] - 2026-09-18

### 画廊滚动条拖拽跳动修复与极速尺寸嗅探
- 修复画廊中按住鼠标拖动垂直滚动条滑块时，因缩略图解码更新宽高比触发 `FlattenedDisplayItems.Reset` 导致 Avalonia 虚拟化面板清空容器、滑块瞬移乱跳并陷入震荡死循环的问题；
- 新增零分配 `FastImageHeaderReader`，通过流式解析文件头部几百字节至数 KB 数据，毫秒级提取 JPEG (SOF + Exif Orientation)、PNG (IHDR)、HEIC/AVIF (ISOBMFF ispe + irot) 的物理显示尺寸与旋转矫正；
- 相册扫描阶段直接填入精确显示宽高比与分辨率，首屏自适应等高行即为最终布局形态，彻底杜绝缩略图解码过程中的宽高比突变与无效重排；
- 引入视口滚动状态守卫（`IsUserScrolling`）与 250ms 空闲防抖，用户在拖拽滚动条或滚轮滚动期间严格挂起集合重建，杜绝 `Reset` 打断虚拟化面板；
- 列表流重建前后增加视口垂直偏移记忆与平滑恢复，避免 Avalonia 默认回滚顶部；
- 显式禁用画廊横向滚动条干扰，为 `GalleryListBox` 配置独立虚拟化面板；
- 扩充单元测试与回归测试用例至 402 项全绿。

---

## [3.0.2] - 2026-09-18

### 依赖自动发现与下载链路修复
- 修复外部依赖下载至应用 `tools/` 目录后，业务转换与悬浮播放模块因未读取设置路径且 `ToolLocator` 未扫描 `tools/` 子目录导致依然提示“依赖缺失”的问题；
- 在 `ToolLocator.Find` 默认候选路径中自动纳入 `tools/` 子目录，并在 `ConvertViewModel`、`StripViewModel` 与 `PlaybackHost` 中全链路打通设置中的自定义引擎路径；
- 将 `ToolLocator.IsValidTool` 启动探测超时从 2s 放宽至 6s，避免 Windows Defender 实时查杀或 ExifTool Perl 环境首次自解压冷启动超时被误判为未安装；
- 优化 GitHub 加速镜像 URL 拼接逻辑，剥离源已有代理前缀彻底杜绝双重代理，并将 ExifTool 备选源修正为 SourceForge 官方 Windows 二进制包。

---

## [3.0.1] - 2026-09-12

### 大相册启动崩溃热修复
- 修复千张级相册恢复缩略图缓存时，Skia 像素分配失败异常穿透 UI 线程并终止程序的问题；失败项目现在安全降级为占位图并进入后台重试；
- 为无损预览建立严格内存预算：缩略图最多驻留 24 张，悬浮视频仅缓存 1 组 60 帧，首屏只预热 1 段视频，QuickLook 最多保留 90 帧；保持 720p/1080p 清晰度不变；
- 移除 QuickLook FFmpeg 的不稳定自动硬件加速探测，改用安全参数列表启动，避免部分环境中的 FFmpeg 异常；
- 增加位图分配失败隔离、缓存预算和解码参数回归测试，并以 1,159 张相册配置完成 Native AOT 启动稳定性验证。

---

## [3.0.0] - 2026-09-12

### 重大重构：从 CLI 命令行工具升级为 GUI 桌面应用
- 项目完成产品形态重构：移除 `LivePhotoConvert.Cli`、Spectre.Console 命令与终端交互向导，全部能力迁移至基于 **Avalonia 12** 与 **CommunityToolkit.Mvvm** 构建的现代桌面图形界面；
- 原有 `merge`、`split`、`strip`、`tools` 等命令行工作流重新组织为「实况互转」「空间瘦身」「依赖引擎」「批次报告」四个可视化业务模块，无需记忆参数即可完成目录选择、模式配置、批量处理与结果查看；
- 新增相册式工作台：自动扫描并配对照片与视频，支持按日期分组、排序、筛选、缩略图浏览、悬浮播放、快速查看，以及配对异常时的人工裁决；
- 转换选项、HEIC 画质、并发数、主题和语言均可在界面中配置并持久化；ExifTool、FFmpeg 与 heif-enc 可视化检测、下载和管理；
- 底层转换能力统一沉淀至无 UI 依赖的 `LivePhotoConvert.Core`，桌面层只负责交互与任务编排，继续保持流式处理、原子写入、源文件保护和 Native AOT 兼容；
- 发布物由命令行程序切换为 Windows x64 Native AOT 桌面应用。**这是不兼容升级：3.0.0 起不再提供旧版 CLI 入口和命令行参数。**

### 桌面体验与响应式相册
- 完成 Avalonia 桌面工作流的界面打磨，统一主窗口、偏好设置、关于页与依赖引擎管理的视觉层级和交互样式；
- 相册升级为接近 Windows 照片的自适应等高布局：依据视口、原图比例与小/中/大目标行高动态分行，完整行铺满且不拉伸图片，窗口缩放时实时重排；
- 修正卡片圆角、状态标签留白、全屏布局和真实宽高比延迟回填等问题，并补齐日期分组、排序、折叠和筛选回归覆盖。

### 实况预览质量与性能
- 悬浮播放与快速查看改用 720p Lanczos、BMP/BGR24 无损帧管线，并以 `fps_mode=passthrough` 保留源视频节奏，修复高名义帧率 MOV 的慢动作和重复帧；
- 增加渐进首帧展示、视口预热、缩略图及视频帧 LRU 管理、延迟释放与取消隔离，降低大相册滚动和连续预览时的延迟与内存压力；
- 快速查看异步加载最高 1600px 的高清静态预览，同时保持播放帧切换与资源释放安全。

### Motion Photo 兼容性
- 从 Apple Live Photo 的 `StillImageTime` 定时元数据轨道还原真实封面帧时间戳，并写入现代与旧版 GCamera XMP 字段，替代固定 1.5 秒值；
- 保留小米 `0x8897` 兼容标签并完善现代 `MotionPhoto + Container` 目录信息，提升 HyperOS / MIUI 相册识别与播放可靠性；
- 同步更新中英文协议说明，并为精确时间戳解析、合并写入与桌面播放参数增加自动化测试。

### 发布验证
- 发布入口统一为 `LivePhotoConvert.Desktop`，继续提供 Windows x64 Native AOT 绿色压缩包；
- Release 构建、完整测试、Native AOT 发布与归档流程均纳入 3.0.0 发布前验证。

---

## [2.6.0] - 2026-08-25

### Apple 实况还原优化（`split -f apple`）
- **原生大写扩展名**：输出统一为 Apple 原生 `.HEIC` + `.MOV` 配对；
- **封面自动转码 HEIC**：源封面为 JPEG 时自动转码为 HEIC（可调 `-q/--quality`，默认 90），转码保留 EXIF/GPS/相机元数据；
- **原生 PCM 音频**：视频重封装/转码音频统一为 `pcm_s16le`（Apple 原生 `lpcm`/`sowt`），无音轨时自动跳过；
- **QuickTime 深度元数据同步**：通过 `-tagsFromFile` 将照片的 `DateTimeOriginal`、`GPSPosition`、`Make`、`Model`、`Software` 完整同步为视频的 `CreateDate`、`MediaCreateDate`、`Keys:CreationDate`、`Keys:GPSCoordinates` 等标签；
- **修复 QuickTime 时间戳时区偏移**：写入 mvhd/mdhd 时间时显式启用 `-api QuickTimeUTC=1`，确保与 Apple 原片一致按 UTC 存储。

### HEIC 默认画质提升（65 → 90）
- 依据 libheif/heif-enc 视觉无损区间（80~90），全链路默认画质统一为 90（接口契约、领域模型、CLI、交互提示、双语文档）；
- 交互模式支持手动输入 1-100 质量值（回车使用默认 90）；
- 新增质量范围校验（1-100），越界参数即时报错。

### 合成阶段全量元数据复制
- HEIC/PNG 封面转码 JPEG 后调用 `CopyAllTagsAsync`（`-all:all -unsafe -icc_profile`），确保 Apple MakerNotes、镜头参数、Display P3 ICC 配置完整复制，杜绝元数据丢失与转码偏色。

### 其它修复
- `tools` 命令补齐 `--heif-enc` 路径透传；
- 移除无效的 `-Keys:StillImageTime=0` 写入（ExifTool 非可写标签，Apple 原片亦无此键）；
- 更正文档与注释中 Apple 视频端配对描述及重封装措辞。

### 测试
- 补充 Apple 大写扩展名、HEIC 转码与元数据同步断言，单元测试全量通过。

---

## [2.5.0] - 2026-08-18

### 新增：动态照片瘦身优化 (`strip` 命令)
- **批量剥离动态照片内嵌视频**：自动探测动态照片中的微视频偏移量（`MicroVideoOffset` / `DirectoryItemLength`），无损剥离内嵌 MP4 视频并清洗 XMP/EXIF 动态标签（含小米 `0x8897` 标识），还原为纯静态图片；
- **转换为 HEIC 高效格式**：集成基于 `libheif` / `x265` 的外部独立编码器 `heif-enc`，支持可调节压缩质量（默认质量 65），在纯图片基础上进一步缩减 50%~70% 体积；
- **100% 保留文件系统时间戳**：精准同步原始动态照片的创建时间（`CreationTime`）与修改时间（`LastWriteTime`），即使转码并重命名为 `.heic`，相册与资源管理器中的拍摄年份与排序完全不受影响；
- **完整保留 EXIF 拍摄元数据**：相机型号（iPhone/小米等）、镜头参数、GPS 经纬度/海拔、拍摄时间等 EXIF 信息全部高保真保留；
- **支持就地修改与输出目录**：默认支持就地覆盖替换（最省空间，临时目录中完成后安全原子替换原文件，异常时不损坏），亦支持 `-o / --output` 输出到指定目录；
- **支持仅剥离视频**：支持 `--no-heic` 参数跳过格式转换，仅移除内嵌视频保持 JPG 格式；
- **实测验证**：在 100 张真实小米动态照片测试集上全量验证，成功率 100%，总体积从 `603.46 MB` 压缩至 `194.12 MB`，节省存储空间达 `409.34 MB`（综合释放率 67.8%，部分长视频照片释放率达 96.2%）。

### 外部工具生态扩展 (`heif-enc`)
- **自动检测与下载**：在 `ToolLocator` 与 `ToolDownloader` 中新增 `heif-enc` 支持，内置国内 GitHub 加速镜像（`ghfast.top`、`ghproxy.net`）与官方源；
- **Windows .7z 原生解压支持**：扩展 `ToolDownloader` 支持自动调用 Windows 内置 `tar.exe` 完整解压 `.7z` 运行库包并扁平化部署依赖 DLL。

---

## [2.4.0] - 2026-08-15

### 命令行框架现代化（Spectre.Console.Cli）
- **消除手写重复解析器**：彻底移除手写的 250 行 `CliParser.cs` 与 100 行硬编码 `HelpText.cs`；
- **声明式命令与强类型参数**：全面接入 `Spectre.Console.Cli` 框架，重构 `merge`、`split`、`tools` 独立子命令与 `Settings` 类，提供强类型校验与格式约束；
- **终端 UI 升级**：自动生成现代、自适应终端换行、具备丰富色彩与语法高亮的 `--help` 帮助界面；
- **双模体验保留**：无参数直接启动时自动进入交互式菜单向导，带参数时直接调度 CLI 流水线。

### Windows 原生现代弹窗与高分屏优化（FolderPicker）
- **查看文件夹内部内容**：移除 Windows 强制仅显示文件夹的限制，双击进入目录时完整显示内部所有照片、视频及其缩略图，支持直接确认当前目录或选中任一文件确认；
- **高分屏极致清晰（PerMonitorV2）**：集成 `app.manifest` 与运行时 API，开启 Windows 原生每显示器高 DPI 感知，彻底杜绝 2K/4K 屏幕及高缩放比（125%~200%）下窗口和字体的发虚与模糊；
- **修复取消回退逻辑**：精准捕获用户主动点击取消事件，杜绝取消后二次弹出旧版树形对话框。

### 消除重复造轮子与 BCL 现代化
- **回收站安全删除**：废除 30 年前过时的非托管 Win32 `SHFileOperationW` 调用与 `ShFileOpStruct` 结构体，直接调用 .NET BCL 官方标准库 `Microsoft.VisualBasic.FileIO.FileSystem.DeleteFile`；
- **进程管理调度升级**：利用 .NET 10 原生 `ProcessStartInfo.ArgumentList` 与 `WaitForExitAsync(cancellationToken)` 异步双流读取，并在取消时强制销毁整个进程树（`entireProcessTree: true`），杜绝孤儿进程驻留；
- **公共 I/O 抽象抽离**：提取 `FileHelper.TryDeleteFile` / `TryDeleteDirectory` 与 `FileTimestamp.Sync` / `FileTimestamp.SyncEarliest`，消除各业务模块重复编写的清理和时间戳同步逻辑。

### 高性能与零堆内存分配优化
- **Frozen 极速集合**：扩展名检查与优先级映射升级为 `FrozenSet<string>` 与 `FrozenDictionary<string, int>`，检索提速至 $O(1)$；
- **零分配格式嗅探**：使用 UTF-8 字节切片（`"heic"u8`、`"qt  "u8`）与位运算 `(b | 0x20)` 进行无堆分配魔数比对；
- **流式物理预分配与内存池**：在 `BinaryFile` 流复制与拼接中引入 `PreallocationSize` 预分配与 `ArrayPool<byte>.Shared` 内存租借，降低 NTFS 磁盘碎片并消除大文件拼接 GC 压力。

### 测试与真实相册全量验证
- **单元测试扩充**：全量单元测试 100% 通过；
- **真实数据实测**：在 100 组真实 iPhone 实况照片（HEIC+MOV、JPG+MOV、UUID 名）上实测批量合成、Android 解包、Apple 重构等 6 种场景，合成与拆分成功率均达 100%。

---

## [2.3.0] - 2026-08-14

### 修复：前置摄像头自拍视频方向颠倒

**问题**：iPhone 前置摄像头拍摄的实况照片，合成动态照片后内嵌视频方向错误——竖屏拍摄左右颠倒、横屏拍摄上下颠倒。

**根因**：前置摄像头视频的 QuickTime 变换矩阵**含镜像**（行列式为负），而后置摄像头为纯旋转（行列式为正）。旧版合成 MP4 时一律走 `-c:v copy` 无损换容器，镜像矩阵被原样保留，而安卓相册等 MP4 播放器不识别镜像矩阵，导致视频方向错乱。

**修复**：合成前用 ExifTool 读取变换矩阵并计算行列式 `det = a*d - b*c`，检测到镜像（`det < 0`）时改走 FFmpeg 重新编码，由 autorotate 把镜像与旋转一起烧进像素；后置摄像头视频仍保持无损换容器，画质与速度不受影响。

### 修复：同名但不同内容的照片被错误配对

**问题**：目录里存在同名但不同内容的照片（如 `IMG_0435.JPG` 缩略图 与 `IMG_0435.JPEG` 原图）时，旧版按扩展名盲选，会把不相关的照片和视频封装成一张动态照片。

**修复**：
- **收紧配对校验（PairValidator）**：
  - 仅单边存在 ContentIdentifier → 直接拒绝；
  - 拍摄时间差超过 3 秒 → 直接拒绝（原为「可疑」）；
  - 拍摄时间仅单边存在 → 直接拒绝；
  - 视频时长超过 30 秒 → 直接拒绝。
  - ContentIdentifier 一致仍为最强正向信号，直接通过；所有维度均不可评估时才降级为仅文件名匹配。
- **同名多候选智能选择**：保留同名多格式候选并按扩展名优先级排序，合成阶段逐个校验、每组只取第一个通过者。

### 改进：按 ContentIdentifier 精确配对

**问题**：从 iCloud 下载的实况照片，照片与视频**文件名会错位**（如照片 `IMG_0011-1.JPEG`、视频 `IMG_0011.MOV`），按文件名配对会漏掉；且会出现**同名不同内容**（如 `IMG_0456.JPG` 缩略图 与 `IMG_0456.JPEG` 原图，ContentIdentifier 不同）互相覆盖输出。

**修复**：
- 配对逻辑升级为**优先按 ContentIdentifier 精确配对**（同一张实况照片的照片与视频该值相同，且不受文件名影响），剩余无法按 CI 配对的再按文件名兜底；
- CI 配对结果是确定的 1:1 配对，直接采用，不再参与同名候选合并；
- 同名但不同内容的照片，输出名用扩展名区分（如 `MVIMG_IMG_0456.jpeg.jpg` / `MVIMG_IMG_0456.jpg.jpg`），避免互相覆盖；
- 合成前批量读取所有照片与视频的 ContentIdentifier，供精确配对使用。

- **性能与鲁棒性优化**：
  - 校验器直接复用预扫描的 ContentIdentifier 内存字典，消除合成校验阶段的重复 ExifTool 进程通信；
  - 增强 JPEG 头部特征魔数检测（`0xFF 0xD8 0xFF`），自动识别并转码被错误重命名为 `.jpg` 的非 JPEG 图片；
  - 修复多格式优选时被替代项被误记为跳过失败的问题，校准总数与进度条同步；
  - 强化取消令牌（`CancellationToken`）响应与跨线程内存可见性。
- **单元测试**：`dotnet test` 108/108 全量通过（新增 CI 缓存穿透、魔数识别、多格式优选状态、取消异常透传等用例）。
- **真实相册验证**：实测全部正确合并，封面照片与内嵌视频一一对应（0 误合并、0 漏合并、0 覆盖丢失）。

---

## [2.2.0] - 之前版本

（历史版本说明略，可参考 git 提交记录）
